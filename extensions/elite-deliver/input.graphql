query Input {
  cart {
    buyerIdentity {
        customer {
            id
            tagged: hasAnyTag(tags: ["elite"])
        }
    }
    deliveryGroups {
      id
      deliveryOptions {
        handle
        title
      }
    }
  }
}